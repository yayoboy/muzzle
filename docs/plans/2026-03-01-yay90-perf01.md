# YAY-90 PERF-01 xterm.js Scrollback Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Set `scrollback: 50000` on the xterm.js instance to allow 50k lines of terminal history.

**Architecture:** One-line change in `apps/web/src/components/Terminal.tsx` — add `scrollback: 50000` to the `XTerm` constructor options object. xterm.js handles the circular buffer internally.

**Tech Stack:** xterm.js (`@xterm/xterm` v6), TypeScript, Next.js.

---

## Task 1: Add scrollback option + type-check + commit

**Files:**
- Modify: `apps/web/src/components/Terminal.tsx:34-42`

### Step 1: Read the file to confirm current state

```bash
grep -n "scrollback\|new XTerm" /Users/yayoboy/Desktop/GitHub/muzzle/apps/web/src/components/Terminal.tsx
```

Expected output: `new XTerm({` at line ~34, no `scrollback` line present.

### Step 2: Apply the change

In `apps/web/src/components/Terminal.tsx`, find the `new XTerm({` block:

```ts
const xterm = new XTerm({
  cursorBlink: true,
  fontSize: 14,
```

Replace with:

```ts
const xterm = new XTerm({
  cursorBlink: true,
  scrollback: 50000,
  fontSize: 14,
```

Use the Edit tool — `old_string` is `cursorBlink: true,\n      fontSize: 14,`, `new_string` is `cursorBlink: true,\n      scrollback: 50000,\n      fontSize: 14,`.

### Step 3: Type-check

```bash
cd /Users/yayoboy/Desktop/GitHub/muzzle/apps/web && npx tsc --noEmit 2>&1
```

Expected: no errors. (`scrollback` is a valid `ITerminalOptions` field in xterm.js v6.)

### Step 4: Commit

```bash
cd /Users/yayoboy/Desktop/GitHub/muzzle
git add apps/web/src/components/Terminal.tsx
git commit -m "feat(yay-90): set xterm scrollback to 50000 lines"
```

---

## Task 2: Mark YAY-90 as Done on Linear

Mark https://linear.app/yayoboy/issue/YAY-90 as Done.
